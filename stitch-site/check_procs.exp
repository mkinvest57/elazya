#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[0] ?$}

# Check processes
send "ps -u su594661\r"
expect -re {[0] ?$}
send "ps aux | grep node\r"
expect -re {[0] ?$}

# Check disk usage (maybe quota full?)
send "quota -s\r"
expect -re {[0] ?$}
send "du -sh .\r"
expect -re {[0] ?$}

send "exit\r"
