#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[$#] ?$}

# Create test php file
send "echo '<?php echo \"PHP Works\"; ?>' > test.php\r"
expect -re {[$#] ?$}

send "exit\r"
