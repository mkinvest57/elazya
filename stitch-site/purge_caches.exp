#!/usr/bin/expect -f
set timeout 120
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[$#] ?$}

# Purge heavy folders
send "rm -rf .npm .cache .pm2\r"
expect -re {[$#] ?$}

send "rm -rf elazya elazya_backup downloads\r"
expect -re {[$#] ?$}

# Also clean _next and out just to be safe and start fresh
send "rm -rf _next out public\r"
expect -re {[$#] ?$}

# Check usage
send "du -sh .\r"
expect -re {[$#] ?$}

# Verify hello.html still exists
send "ls -l hello.html\r"
expect -re {[$#] ?$}

send "exit\r"
