#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[$#] ?$}

# Rename .htaccess to disable it
send "mv .htaccess .htaccess_bak\r"
expect -re {[$#] ?$}

# Check if logs/ exists
send "ls -F logs/\r"
expect -re {[$#] ?$}

send "exit\r"
