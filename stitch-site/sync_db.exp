#!/usr/bin/expect -f
set timeout 60
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}

# Go to app dir
send "cd elazya-server\r"
expect "$ "

# Run Prisma Push (Sync DB schema)
# Note: We need to use the DATABASE_URL from .env
send "npx prisma db push\r"
expect "$ "

send "exit\r"
