#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[$#] ?$}

send "ls -la\r"
expect -re {[$#] ?$}

send "ls -la elazya/\r"
expect -re {[$#] ?$}

send "cat elazya/.htaccess\r"
expect -re {[$#] ?$}

send "exit\r"
