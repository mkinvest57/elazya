#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[0] ?$}
send "ls -la\r"
expect -re {[0] ?$}
send "ls -la public_html 2>/dev/null || echo 'no public_html'\r"
expect -re {[0] ?$}
send "ls -la www 2>/dev/null || echo 'no www'\r"
expect -re {[0] ?$}
send "exit\r"
