#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[$#] ?$}

send "cd elazya-server\r"
expect -re {[$#] ?$}

# Re-run check
send "node check_prisma.js > prisma.log 2>&1\r"
expect -re {[$#] ?$}

send "cat prisma.log\r"
expect -re {[$#] ?$}

send "exit\r"
