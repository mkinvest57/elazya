#!/usr/bin/expect -f
set timeout 60
spawn sftp su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect "sftp>"

# Clean target
send "rm elazya/*\r"
expect "sftp>"

# Upload static tar to root (easier)
send "put static.tar.gz\r"
expect "sftp>"

send "bye\r"
