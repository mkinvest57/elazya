#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[$#] ?$}

# Create test file
send "echo 'Hello World check' > test_connection.txt\r"
expect -re {[$#] ?$}

# Restore .htaccess (since it wasn't the cause)
send "mv .htaccess_bak .htaccess\r"
expect -re {[$#] ?$}

send "exit\r"
