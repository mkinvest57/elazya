#!/usr/bin/expect -f
set timeout 600
spawn sftp su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect "sftp>"

# Clean old static.tar.gz if exists
send "rm static.tar.gz\r"
expect "sftp>"

# Put new one
send "put static.tar.gz\r"
expect "sftp>"

send "bye\r"
