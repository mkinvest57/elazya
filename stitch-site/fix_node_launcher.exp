#!/usr/bin/expect -f
set timeout 60
spawn sftp su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect "sftp>"
send "put start_node_local.php elazya/start_node2.php\r"
expect "sftp>"
send "bye\r"

spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
}
expect -re {[$#] ?$}

# Restart the process using HTTP webhook
send "curl -s http://localhost/start_node2.php\r"
expect -re {[$#] ?$}

send "sleep 2\r"
expect -re {[$#] ?$}

send "ps ux | grep node\r"
expect -re {[$#] ?$}

send "exit\r"

