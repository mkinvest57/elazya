#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[0] ?$}

send "cat start_node.php\r"
expect -re {[0] ?$}

send "cat package.json\r"
expect -re {[0] ?$}

send "ls -la logs/\r"
expect -re {[0] ?$}

send "exit\r"
