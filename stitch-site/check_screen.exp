#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[$#] ?$}

send "which screen\r"
expect -re {[$#] ?$}

send "execute_this_command_if_screen_exists"
# Wait, expect logic:
# If screen exists, output path.
# If not, output nothing.

send "exit\r"
