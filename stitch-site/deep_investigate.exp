#!/usr/bin/expect -f
set timeout 30
spawn ssh su594661@access-5019523613.webspace-host.com
expect {
    "password:" { send "Yassine.2011\r" }
    "(yes/no)?" { send "yes\r"; exp_continue }
}
expect -re {[0] ?$}

# Check history for setup commands
send "tail -n 50 .bash_history\r"
expect -re {[0] ?$}

# Test internal access
send "curl -v http://localhost/test.html\r"
expect -re {[0] ?$}

# Check logs symlink?
send "ls -la logs\r"
expect -re {[0] ?$}

# Check where we are exactly
send "pwd\r"
expect -re {[0] ?$}
send "id\r"
expect -re {[0] ?$}

send "exit\r"
